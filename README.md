# icepi-memtest
A memory tester for the icepi zero FPGA board. Generates and reads back a bunch of random bits at every memory address. Runs as fast as I could make it run.

The controller itself is in sramInterface.sv
